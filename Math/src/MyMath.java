public class MyMath {

    boolean isEven(int x) {
        return (x % 2 == 0);
    }

    boolean isOdd(int x) {
        return (x % 2 != 0);
    }

    double circleField(double x) {
        return Math.PI * Math.pow(x, 2);
    }

    int power(int x) {
        return x * x;
    }
}
